export interface IPreappIdData {
    data: { preappId: string; redirectLink: string; status: string };
}

export interface IPreappId {
    preappId: string;
}

export interface ICode {
    code: string;
}
